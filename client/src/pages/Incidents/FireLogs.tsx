import { FC } from 'react'
import SideBar from '../../components/SideBar'

const FireLogs: FC = () => {
    return (
        <div className='flex'>
          <SideBar />
          Fire
        </div>
      )
}

export default FireLogs
