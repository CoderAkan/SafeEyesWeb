import { FC } from 'react'
import SideBar from '../components/SideBar'

const Settings: FC = () => {
  return (
    <div className='flex'>
        <SideBar />
        Settings page
    </div>
  )
}

export default Settings
